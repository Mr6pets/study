let val;
val = Math.PI;

val = Math.round(2.6);//四舍五入
val = Math.ceil(2.8);//向上取整
val = Math.floor(2.8);//向下取整
val = Math.sqrt(64);//平方
val = Math.abs(-3);//取绝对值
val = Math.min(2, 3, 5, 6, 7);//最小值
val = Math.max(2, 3, 5, 6, 7, 8, 54, 5);//最大值

val = Math.random() * 20 + 1;//(1~20)

console.log(val);

